import { createMuiTheme } from 'material-ui/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#ffbd45',
      main: '#fb8c00',
      dark: '#c25e00',
      contrastText: '#000000',
    },
  }
})

export default theme
